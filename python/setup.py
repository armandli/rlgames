from setuptools import setup, find_packages

setup(
  name='rlgames',
  version='0.0.1',
  description='Reinforcement learning games',
  author='armandli',
  author_email='armand.li@hotmail.com',
  packages=find_packages(),
  package_data={},
  install_requires=[],
  entry_points={},
)
